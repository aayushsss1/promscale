---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: inferencescalers.autoscaling.mlop.io
spec:
  group: autoscaling.mlop.io
  names:
    kind: InferenceScaler
    listKind: InferenceScalerList
    plural: inferencescalers
    singular: inferencescaler
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InferenceScaler is the Schema for the inferencescalers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InferenceScalerSpec defines the desired state of InferenceScaler.
            properties:
              behavior:
                properties:
                  pollIntervalSeconds:
                    default: 15
                    format: int32
                    minimum: 5
                    type: integer
                  scaleDown:
                    properties:
                      cooldownSeconds:
                        format: int32
                        minimum: 0
                        type: integer
                      maxStep:
                        format: int32
                        minimum: 1
                        type: integer
                      stabilizationWindowSeconds:
                        description: |-
                          ScaleDown only: prevents flapping by using the max recommended replicas
                          observed in this lookback window.
                        format: int32
                        minimum: 0
                        type: integer
                    type: object
                  scaleUp:
                    properties:
                      cooldownSeconds:
                        format: int32
                        minimum: 0
                        type: integer
                      maxStep:
                        format: int32
                        minimum: 1
                        type: integer
                      stabilizationWindowSeconds:
                        description: |-
                          ScaleDown only: prevents flapping by using the max recommended replicas
                          observed in this lookback window.
                        format: int32
                        minimum: 0
                        type: integer
                    type: object
                type: object
              maxReplicas:
                format: int32
                minimum: 1
                type: integer
              minReplicas:
                default: 1
                format: int32
                minimum: 0
                type: integer
              prometheus:
                properties:
                  timeoutSeconds:
                    default: 5
                    format: int32
                    minimum: 1
                    type: integer
                  url:
                    type: string
                required:
                - url
                type: object
              safeguards:
                properties:
                  missingMetricsPolicy:
                    default: Hold
                    description: Hold | ScaleToMin | Error
                    enum:
                    - Hold
                    - ScaleToMin
                    - Error
                    type: string
                  readiness:
                    properties:
                      enabled:
                        default: true
                        type: boolean
                      minReadySecondsAfterScaleUp:
                        default: 60
                        description: Prevent scale-down for N seconds after a scale-up.
                        format: int32
                        minimum: 0
                        type: integer
                    type: object
                type: object
              signals:
                items:
                  properties:
                    name:
                      minLength: 1
                      type: string
                    query:
                      description: PromQL query expected to return a single scalar.
                      minLength: 1
                      type: string
                    strategy:
                      properties:
                        perReplica:
                          properties:
                            target:
                              description: Target value per replica (e.g., queue depth
                                per pod).
                              pattern: ^[0-9]+(\.[0-9]+)?$
                              type: string
                          required:
                          - target
                          type: object
                        threshold:
                          properties:
                            above:
                              description: Above triggers the action if the metric
                                is greater than this value.
                              pattern: ^[0-9]+(\.[0-9]+)?$
                              type: string
                            action:
                              properties:
                                scaleDownBy:
                                  format: int32
                                  minimum: 0
                                  type: integer
                                scaleUpBy:
                                  format: int32
                                  minimum: 0
                                  type: integer
                                type:
                                  description: ScaleUpBy | ScaleDownBy | Noop
                                  enum:
                                  - ScaleUpBy
                                  - ScaleDownBy
                                  - Noop
                                  type: string
                              required:
                              - type
                              type: object
                            below:
                              description: Below triggers the action if the metric
                                is less than this value.
                              pattern: ^[0-9]+(\.[0-9]+)?$
                              type: string
                          required:
                          - action
                          type: object
                        type:
                          description: PerReplica | Threshold
                          enum:
                          - PerReplica
                          - Threshold
                          type: string
                      required:
                      - type
                      type: object
                  required:
                  - name
                  - query
                  - strategy
                  type: object
                type: array
              targetRef:
                properties:
                  apiVersion:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
            required:
            - maxReplicas
            - prometheus
            - signals
            type: object
          status:
            description: InferenceScalerStatus defines the observed state of InferenceScaler.
            properties:
              conditions:
                description: |-
                  Conditions represent the current state of the scaler using standard
                  Kubernetes condition semantics.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentReplicas:
                description: CurrentReplicas is the replica count currently set on
                  the target.
                format: int32
                type: integer
              desiredReplicas:
                description: DesiredReplicas is the last desired replica count computed
                  by PromScale.
                format: int32
                type: integer
              lastPollTime:
                description: LastPollTime is when the scaler last evaluated metrics
                  and policy.
                format: date-time
                type: string
              lastScaleTime:
                description: LastScaleTime is when the scaler last changed the target
                  replicas.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the last generation of the spec
                  that was processed.
                format: int64
                type: integer
              readyReplicas:
                description: ReadyReplicas is the number of replicas that are ready
                  to serve traffic.
                format: int32
                type: integer
              recommendations:
                description: |-
                  Recommendations stores recent desired replica recommendations.
                  Used for stabilization windows to prevent flapping.
                items:
                  description: ReplicaRecommendation represents a historical desired
                    replica count computed by the scaling policy.
                  properties:
                    desiredReplicas:
                      format: int32
                      type: integer
                    timestamp:
                      format: date-time
                      type: string
                  required:
                  - desiredReplicas
                  - timestamp
                  type: object
                type: array
              signals:
                description: Signals records the last observed values for each configured
                  signal.
                items:
                  description: ObservedSignal captures the last observed value of
                    a scaling signal.
                  properties:
                    name:
                      type: string
                    timestamp:
                      format: date-time
                      type: string
                    value:
                      type: string
                  required:
                  - name
                  - timestamp
                  - value
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
